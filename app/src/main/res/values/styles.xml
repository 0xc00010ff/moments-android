<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@color/background</item>
        <item name="android:actionMenuTextAppearance">@style/CustomActionBar</item>
    </style>

    <style name="MakeAMomentTheme" parent="AppTheme">
        <item name="android:actionMenuTextColor">@color/textLight</item>
    </style>

    <!--style exists only to change the size of the text in the make_a_moment_menu-->
    <style name="CustomActionBar" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Menu">

        <item name="android:textSize">18sp</item>

    </style>

</resources>
