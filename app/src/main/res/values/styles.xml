<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@color/background</item>
        <item name="android:actionMenuTextAppearance">@style/CustomActionBar</item>
        <item name="android:actionMenuTextColor">@drawable/menu_text_color</item>
        <item name="md_dark_theme">false</item>
        <item name="md_title_gravity">center</item>
        <item name="md_content_gravity">center</item>
        <item name="md_items_gravity">center</item>

    </style>

    <!--style exists only to change the size and color of the text in the MakeAMomentActivity and subsequent Activities-->
    <style name="CustomActionBar" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Menu">

        <item name="android:textSize">18sp</item>
        <item name="android:actionMenuTextColor">@color/textLight</item>

    </style>

</resources>
